# VYTA Runtime 0.1
Provider-independent, self-host-first inference runtime.

Current endpoints:
- GET /health
- POST /v1/route

This first deployment intentionally routes in shadow mode and does not execute model inference yet.
