import {InMemoryCreativeRepository} from '../persistence/repository';import {StudioApi} from '../api/studio-api';
async function test(){const repo=new InMemoryCreativeRepository(),api=new StudioApi(repo),now=new Date().toISOString();await api.createProject({id:'MAK-CR-0001',brand_id:'MAKSTER_ATELIER',status:'DRAFT',payload:{objective:'test'},version:1,created_at:now,updated_at:now});if((await api.listProjects('VYTA')).length!==0)throw new Error('Brand list isolation failed');const p=await api.patchProject('MAK-CR-0001','MAKSTER_ATELIER',1,{objective:'updated'});if(p.version!==2)throw new Error('Optimistic versioning failed');let conflict=false;try{await api.patchProject('MAK-CR-0001','MAKSTER_ATELIER',1,{x:true});}catch{conflict=true;}if(!conflict)throw new Error('Expected version conflict');}void test();
