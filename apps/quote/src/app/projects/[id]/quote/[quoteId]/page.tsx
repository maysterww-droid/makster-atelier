import Link from 'next/link';
import { notFound } from 'next/navigation';
import { formatMinor } from '@/lib/calculation';
import { getInterfaceLocale } from '@/lib/interface-locale';
import { INTL_LOCALES } from '@/lib/i18n';
import { getDeliveryMessages } from '@/lib/i18n-delivery';
import { effectiveQuoteStatus, quoteActionAllowed, quoteReference } from '@/lib/quote-lifecycle';
import { minorFromUnknown } from '@/lib/project-pricing';
import { readQuoteSnapshot } from '@/lib/quote-snapshot';
import { requireWorkspace } from '@/lib/workspace';
import { appendQuoteStatus } from '../../publish-actions';
import { ClientDeliveryPanel } from './client-delivery-panel';
import { revokeClientLink } from './delivery-actions';

export const dynamic='force-dynamic';
type Props={params:Promise<{id:string;quoteId:string}>;searchParams:Promise<{saved?:string;error?:string}>};
const actionRoles=new Set(['owner','admin','sales','technologist']);

export default async function PublishedQuotePage({params,searchParams}:Props){
  const {id,quoteId}=await params;const query=await searchParams;const {supabase,organization,role}=await requireWorkspace();const locale=await getInterfaceLocale();const m=getDeliveryMessages(locale);const intl=INTL_LOCALES[locale];
  const [quoteResult,eventResult,linksResult,deliveriesResult]=await Promise.all([
    supabase.from('client_commercial_quotes').select('id, project_id, quote_version, issued_at, valid_until, currency, net_amount_minor, tax_minor, total_amount_minor, quote_json, client_name').eq('id',quoteId).eq('project_id',id).eq('organization_id',organization.id).maybeSingle(),
    supabase.from('client_quote_status_events').select('id, status, note, created_at').eq('quote_id',quoteId).eq('organization_id',organization.id).order('created_at',{ascending:false}).order('id',{ascending:false}),
    supabase.from('quote_client_access_links').select('id, purpose, recipient_email, expires_at, revoked_at, created_at').eq('quote_id',quoteId).eq('organization_id',organization.id).order('created_at',{ascending:false}).limit(20),
    supabase.from('quote_email_deliveries').select('id, recipient_email, provider, status, provider_message_id, error_message, created_at, sent_at').eq('quote_id',quoteId).eq('organization_id',organization.id).order('created_at',{ascending:false}).limit(20),
  ]);
  const quote=quoteResult.data;const events=eventResult.data;if(quoteResult.error||!quote)notFound();if(eventResult.error)throw new Error('Failed to load quote status history.');if(linksResult.error)throw new Error('Failed to load customer links.');if(deliveriesResult.error)throw new Error('Failed to load email delivery log.');
  const parsed=readQuoteSnapshot(quote.quote_json);if(!parsed)throw new Error('Unsupported quote snapshot format.');const snapshot={...parsed,quoteId:quote.id,quoteVersion:quote.quote_version,issuedAt:quote.issued_at,validUntil:quote.valid_until};
  const rawLatest=events?.[0]?.status??'approved';const latest=effectiveQuoteStatus(rawLatest,snapshot.validUntil);const canAct=actionRoles.has(role);const canUpdate=canAct&&quoteActionAllowed(latest,snapshot.validUntil);const canCreateDelivery=canAct&&quoteActionAllowed(latest,snapshot.validUntil);const deposit=minorFromUnknown(snapshot.amounts.depositMinor);const now=Date.now();const activeLinks=(linksResult.data??[]).filter((link)=>!link.revoked_at&&new Date(link.expires_at).getTime()>now);const deliveries=deliveriesResult.data??[];const emailConfigured=Boolean(process.env.RESEND_API_KEY?.trim()&&process.env.QUOTE_EMAIL_FROM?.trim());
  const statusName:Record<string,string>={approved:m.statusApproved,sent:m.statusSent,accepted:m.statusAccepted,rejected:m.statusRejected,expired:m.statusExpired,superseded:m.statusSuperseded};
  const adjustment=snapshot.commercial?.adjustmentMode==='discount'?`${m.discount} ${(snapshot.commercial.adjustmentBps/100).toFixed(2)}%`:snapshot.commercial?.adjustmentMode==='surcharge'?`${m.surcharge} ${(snapshot.commercial.adjustmentBps/100).toFixed(2)}%`:'';
  const money=(value:unknown)=>formatMinor(minorFromUnknown(value),snapshot.currency,intl);const date=(value:string)=>new Intl.DateTimeFormat(intl).format(new Date(value));const dateTime=(value:string)=>new Intl.DateTimeFormat(intl,{dateStyle:'medium',timeStyle:'short'}).format(new Date(value));
  const extraText=(extra:{name:string;quantity?:number;unit?:'m'|'m2'|'job'})=>!extra.quantity||!extra.unit||extra.unit==='job'?extra.name:`${extra.name} · ${extra.quantity.toFixed(2)} ${extra.unit==='m2'?'m²':'m'}`;
  const ref=quoteReference(snapshot.project.id,quote.quote_version);

  return <main className="quoteDocument">
    <style>{`body{background:#eef2ef}.quoteDocument{max-width:920px;margin:24px auto;background:#fff;color:#18211d;padding:48px;box-shadow:0 18px 60px rgba(24,33,29,.12);font-family:Inter,Arial,sans-serif}.toolbar{display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;margin-bottom:30px}.toolbarGroup{display:flex;gap:8px;flex-wrap:wrap}.toolbar a,.toolbar button,.deliveryPanel button,.linkHistory button{border:0;border-radius:9px;padding:10px 13px;text-decoration:none;font-weight:700;cursor:pointer}.secondary{background:#edf2ef;color:#244438}.primary{background:#1f7a5a;color:#fff}.danger{background:#f5e8e8;color:#8b3131}.head{display:flex;justify-content:space-between;gap:30px;border-bottom:2px solid #18211d;padding-bottom:22px}.brand{font-size:25px;font-weight:800;color:#196c50}.brand small{display:block;font-size:11px;color:#68746d;margin-top:4px}.ref{text-align:right;color:#68746d;font-size:12px}.ref strong{display:block;color:#18211d;font-size:18px}.title{margin:34px 0 22px}.title h1{font-size:32px;margin:0}.status{display:inline-block;margin-top:10px;margin-right:7px;border:1px solid #1f7a5a;color:#1f7a5a;padding:5px 9px;border-radius:6px;font-size:11px;font-weight:800}.info{display:grid;grid-template-columns:1fr 1fr;gap:28px;margin:24px 0 32px}.info h3{font-size:11px;text-transform:uppercase;color:#68746d}.info span,.info strong{display:block;margin-top:4px}.table{width:100%;border-collapse:collapse;margin:18px 0 28px}.table th,.table td{text-align:left;padding:11px;border-bottom:1px solid #dfe7e2}.table th{font-size:11px;text-transform:uppercase;color:#68746d}.totals{width:min(440px,100%);margin-left:auto}.totals div{display:flex;justify-content:space-between;padding:8px 0}.totals .grand{border-top:2px solid #18211d;font-size:21px;font-weight:800;padding-top:13px}.terms{margin-top:30px;padding:18px;background:#f4f7f5;border-radius:12px}.terms div{margin:7px 0}.deliveryPanel{margin-top:36px;border-top:1px solid #dfe7e2;padding-top:24px}.deliveryColumns{display:grid;grid-template-columns:1fr 1fr;gap:22px}.deliveryColumns>div{padding:18px;background:#f7f9f8;border-radius:12px}.deliveryColumns h3{margin-top:0}.deliveryColumns p{color:#68746d;font-size:13px;line-height:1.45}.mailForm{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.mailForm input,.shareLine input{flex:1;min-width:210px;padding:10px;border:1px solid #cfd9d3;border-radius:8px;background:#fff}.deliveryConfig{font-size:12px;padding:10px;background:#fff3d6;border-radius:8px;margin-bottom:10px}.deliveryResult{margin-top:12px;padding:11px;border-radius:9px;font-size:12px}.deliveryResult.success{background:#eaf6ef}.deliveryResult.error{background:#faeeee;color:#8b3131}.deliveryResult small{display:block;margin-top:6px}.shareLine{display:flex;gap:8px;margin-top:10px}.linkHistory,.deliveryHistory,.events{margin-top:36px;border-top:1px solid #dfe7e2;padding-top:22px}.historyRow{display:flex;justify-content:space-between;gap:14px;align-items:center;padding:10px 0;border-bottom:1px solid #edf1ef}.historyRow small{display:block;color:#68746d;margin-top:4px}.events article{padding:8px 0}.events small{color:#68746d}.statusForm{margin-top:18px;display:flex;gap:8px;flex-wrap:wrap}.statusForm input[type=text]{flex:1;min-width:220px;padding:10px;border:1px solid #cfd9d3;border-radius:8px}@media(max-width:700px){.quoteDocument{margin:0;padding:24px}.head,.info,.deliveryColumns{display:grid;grid-template-columns:1fr}.ref{text-align:left}.historyRow{align-items:flex-start;flex-direction:column}.shareLine{flex-direction:column}}@media print{body{background:#fff}.quoteDocument{max-width:none;margin:0;padding:14mm;box-shadow:none}.toolbar,.deliveryPanel,.linkHistory,.deliveryHistory,.events{display:none}@page{size:A4;margin:0}}`}</style>
    <div className="toolbar"><div className="toolbarGroup"><Link className="secondary" href={`/projects/${id}`}>{m.projectBack}</Link><Link className="secondary" href={`/projects/${id}/quote`}>{m.livePreview}</Link></div><div className="toolbarGroup"><Link className="primary" href={`/projects/${id}/quote/${quoteId}/pdf`}>{m.downloadPdf}</Link></div></div>
    {query.saved==='status'?<p className="status">{m.statusSaved}</p>:null}{query.error?<p className="status">{m.error}: {query.error}</p>:null}
    <header className="head"><div className="brand">{snapshot.supplier.tradeName}<small>{snapshot.supplier.legalName}</small></div><div className="ref"><strong>{ref}</strong><span>{date(snapshot.issuedAt)}</span><span>{m.until} {date(snapshot.validUntil)}</span></div></header>
    <section className="title"><h1>{m.quotation}</h1><span className="status">{statusName[latest]??latest}</span>{snapshot.commercial?<span className="status">{snapshot.commercial.variantLabel}</span>:null}{adjustment?<span className="status">{adjustment}</span>:null}</section>
    <section className="info"><div><h3>{m.client}</h3><strong>{snapshot.client.name}</strong>{snapshot.client.email?<span>{snapshot.client.email}</span>:null}{snapshot.client.phone?<span>{snapshot.client.phone}</span>:null}{snapshot.client.address?<span>{snapshot.client.address}</span>:null}</div><div><h3>{m.project}</h3><strong>{snapshot.project.name}</strong><span>{m.frozenRevision} {snapshot.project.revisionNumber}</span><span>{m.quoteVersion} {quote.quote_version}</span></div></section>
    <h2>{m.scope}</h2><table className="table"><thead><tr><th>{m.module}</th><th>{m.dimensions}</th><th>{m.qty}</th></tr></thead><tbody>{snapshot.modules.map((module)=><tr key={module.id}><td>{module.name}</td><td>{module.widthMm} × {module.heightMm} × {module.depthMm} mm</td><td>{module.quantity}</td></tr>)}</tbody></table>
    {snapshot.extras.length?<p><strong>{m.included}:</strong> {snapshot.extras.map(extraText).join(' · ')}</p>:null}
    <section className="totals">{snapshot.commercial&&snapshot.commercial.adjustmentMode!=='none'&&snapshot.commercial.adjustmentBps>0?<div><span>{snapshot.commercial.variantLabel} · {m.beforeAdjustment}</span><strong>{money(snapshot.commercial.listNetMinor)}</strong></div>:null}<div><span>{m.net}</span><strong>{money(snapshot.amounts.netMinor)}</strong></div>{snapshot.amounts.taxBps>0?<div><span>{m.tax} {(snapshot.amounts.taxBps/100).toFixed(2)}%</span><strong>{money(snapshot.amounts.taxMinor)}</strong></div>:null}<div className="grand"><span>{m.payable}</span><strong>{money(snapshot.amounts.totalMinor)}</strong></div></section>
    {(snapshot.terms.depositBps>0||snapshot.terms.productionLeadText||snapshot.terms.paymentTerms||snapshot.terms.warrantyText||snapshot.terms.clientNote)?<section className="terms"><h3>{m.terms}</h3>{snapshot.terms.depositBps>0?<div><strong>{m.deposit}:</strong> {(snapshot.terms.depositBps/100).toFixed(2)}% · {formatMinor(deposit,snapshot.currency,intl)}</div>:null}{snapshot.terms.productionLeadText?<div><strong>{m.lead}:</strong> {snapshot.terms.productionLeadText}</div>:null}{snapshot.terms.paymentTerms?<div><strong>{m.payment}:</strong> {snapshot.terms.paymentTerms}</div>:null}{snapshot.terms.warrantyText?<div><strong>{m.warranty}:</strong> {snapshot.terms.warrantyText}</div>:null}{snapshot.terms.clientNote?<div><strong>{m.note}:</strong> {snapshot.terms.clientNote}</div>:null}</section>:null}
    {canCreateDelivery?<ClientDeliveryPanel projectId={id} quoteId={quoteId} defaultEmail={snapshot.client.email} emailConfigured={emailConfigured} locale={locale}/>:null}
    {canAct?<section className="linkHistory"><h3>{m.activeLinks}</h3>{activeLinks.length?activeLinks.map((link)=><div className="historyRow" key={link.id}><div><strong>{link.purpose==='email'?m.emailLink:m.copyLink}</strong><small>{link.recipient_email?`${link.recipient_email} · `:''}{m.created} {dateTime(link.created_at)} · {m.validUntil} {date(link.expires_at)}</small></div><form action={revokeClientLink}><input type="hidden" name="projectId" value={id}/><input type="hidden" name="quoteId" value={quoteId}/><input type="hidden" name="linkId" value={link.id}/><button type="submit" className="danger">{m.revoke}</button></form></div>):<p>{m.noLinks}</p>}</section>:null}
    {canAct?<section className="deliveryHistory"><h3>{m.deliveryLog}</h3>{deliveries.length?deliveries.map((delivery)=><div className="historyRow" key={delivery.id}><div><strong>{delivery.status==='sent'?m.sent:m.sendError} · {delivery.recipient_email}</strong><small>{dateTime(delivery.created_at)}{delivery.provider_message_id?` · ID ${delivery.provider_message_id}`:''}{delivery.error_message?` · ${delivery.error_message}`:''}</small></div><span className="status">{delivery.provider}</span></div>):<p>{m.noDeliveries}</p>}</section>:null}
    <section className="events"><h3>{m.statusHistory}</h3>{events?.map((event)=><article key={event.id}><strong>{statusName[event.status]??event.status}</strong><small>{dateTime(event.created_at)}{event.note?` · ${event.note}`:''}</small></article>)}{canUpdate?<form action={appendQuoteStatus} className="statusForm"><input type="hidden" name="projectId" value={id}/><input type="hidden" name="quoteId" value={quoteId}/><input name="note" type="text" placeholder={m.statusNotePlaceholder}/><button className="secondary" name="status" value="sent">{m.markSent}</button><button className="primary" name="status" value="accepted">{m.clientAccepted}</button><button className="danger" name="status" value="rejected">{m.clientRejected}</button></form>:null}</section>
  </main>;
}
