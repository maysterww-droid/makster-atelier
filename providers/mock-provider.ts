import type { CreativeProviderAdapter,GenerationRequest,GenerationResult,ProviderKind } from './provider-adapter';
export class MockProvider implements CreativeProviderAdapter{readonly name:string;readonly kinds:readonly ProviderKind[];constructor(name='mock',kinds:readonly ProviderKind[]=['IMAGE','VIDEO','VOICE','MUSIC','SFX']){this.name=name;this.kinds=kinds;}async generate(r:GenerationRequest):Promise<GenerationResult>{return{provider:this.name,model:'mock-0',asset_id:`mock://${r.project_id}/${Date.now()}`,cost:0,currency:'USD',metadata:{dry_run:true,brand_id:r.brand_id}};}}
