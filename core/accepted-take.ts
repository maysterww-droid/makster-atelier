import {AssetRegistry} from './assets';import {CostLedger} from './cost-ledger';import type {Shot,Take} from './pipeline';
export function acceptTake(assets:AssetRegistry,costs:CostLedger,projectId:string,brand:'MAKSTER_ATELIER'|'VYTA',shot:Shot,take:Take){if(take.qa_status!=='PASS')throw new Error('TAKE_QA_NOT_PASSED');if(!shot.takes.some(t=>t.id===take.id))throw new Error('TAKE_NOT_ATTACHED');const a=assets.get(take.asset_id,brand);assets.register({...a,approved:true});const entries=costs.forProject(projectId,brand);for(const e of entries)if(e.take_id===take.id&&!e.accepted)costs.add({...e,id:`${e.id}:ACCEPTED`,accepted:true,timestamp:new Date().toISOString()});return{...shot,selected_take_id:take.id};}
