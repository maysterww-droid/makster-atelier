import type {MasterAssembly} from './master-assembly';
export type CaptionCue={id:string;start_seconds:number;end_seconds:number;text:string;locale:string};export type BrandOverlay={logo_asset_id:string;start_seconds:number;end_seconds:number;position:'TOP_LEFT'|'TOP_RIGHT'|'BOTTOM_LEFT'|'BOTTOM_RIGHT'|'CENTER'};export type AudioBed={voice_asset_id?:string;music_asset_id?:string;sfx_asset_ids:string[];voice_gain_db:number;music_gain_db:number;sfx_gain_db:number;duck_music_under_voice:boolean};export type SafeArea={top:number;right:number;bottom:number;left:number};export type ComposedMaster={project_id:string;aspect_ratio:'9:16'|'1:1'|'16:9';duration_seconds:number;assembly:MasterAssembly;captions:CaptionCue[];overlay:BrandOverlay;audio:AudioBed;safe_area:SafeArea;status:'READY_FOR_MASTER_QA'};
const SAFE:Record<ComposedMaster['aspect_ratio'],SafeArea>={'9:16':{top:.08,right:.06,bottom:.16,left:.06},'1:1':{top:.07,right:.07,bottom:.10,left:.07},'16:9':{top:.06,right:.05,bottom:.08,left:.05}};
export function composeMaster(projectId:string,aspect:ComposedMaster['aspect_ratio'],assembly:MasterAssembly,captions:CaptionCue[],overlay:BrandOverlay,audio:AudioBed):ComposedMaster{if(assembly.project_id!==projectId||assembly.status!=='READY_TO_RENDER')throw new Error('ASSEMBLY_NOT_READY');if(!overlay.logo_asset_id)throw new Error('APPROVED_LOGO_REQUIRED');if(overlay.start_seconds<0||overlay.end_seconds>assembly.duration_seconds||overlay.end_seconds<=overlay.start_seconds)throw new Error('INVALID_LOGO_TIMING');for(const c of captions)if(c.start_seconds<0||c.end_seconds>assembly.duration_seconds||c.end_seconds<=c.start_seconds)throw new Error(`INVALID_CAPTION_TIMING:${c.id}`);return{project_id:projectId,aspect_ratio:aspect,duration_seconds:assembly.duration_seconds,assembly,captions,overlay,audio,safe_area:SAFE[aspect],status:'READY_FOR_MASTER_QA'};}
