import type {Shot} from '../core/pipeline';
export type AssemblyClip={shot_id:string;asset_id:string;start_seconds:number;end_seconds:number};export type MasterAssembly={project_id:string;duration_seconds:number;clips:AssemblyClip[];voice_asset_id?:string;music_asset_id?:string;sfx_asset_ids:string[];status:'PLANNED'|'READY_TO_RENDER'};
export function planAssembly(projectId:string,shots:Shot[],duration:number):MasterAssembly{const ordered=[...shots].sort((a,b)=>a.order-b.order);const clips:AssemblyClip[]=[];let cursor=0;for(const s of ordered){const t=s.takes.find(x=>x.id===s.selected_take_id);if(!t||t.qa_status!=='PASS')throw new Error(`ASSEMBLY_SELECTED_TAKE_INVALID: ${s.id}`);const remaining=Math.max(0,duration-cursor);const slice=remaining/(ordered.length-clips.length);clips.push({shot_id:s.id,asset_id:t.asset_id,start_seconds:cursor,end_seconds:cursor+slice});cursor+=slice;}return{project_id:projectId,duration_seconds:duration,clips, sfx_asset_ids:[],status:'READY_TO_RENDER'};}
